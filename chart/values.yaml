# `image` is overridden by deploy.sh via --set image=<registry>:<sha>
image: myapp:latest
replicaCount: 1

service:
  type: ClusterIP
  port: 80
  targetPort: 3000

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 250m
    memory: 128Mi
